# Android native acceptance runner — 2026-09-13

The Community Android preview is now [published as v1.1.33-preview.1](https://github.com/edydex/heritage_study_bible/releases/tag/v1.1.33-preview.1). The accepted run is `34775207954`, source `782535d41e870e12983f807d7f8dda2e217e2c56`. All four native tests passed; all three downloaded screenshots were inspected and show the correct distinct screens without an obscuring dialog. [Package, image and published-asset evidence](2026-09-13-android-preview.json). Earlier failed and rejected runs remain documented below.

The first [package-only run 34769257962](https://github.com/edydex/heritage_study_bible/actions/runs/34769257962), from Heritage `8a0641ccb23eb221e5a5d758607d90b7941b83c4`, built the APK successfully but never reached instrumentation. The recovered `emulator.log` reports `Unknown AVD name [heritage-acceptance]`; `logcat.txt` contains `- waiting for device -`. Cleanup issued unbounded adb commands after emulator startup failed. The obsolete job was explicitly cancelled after that cleanup path had been independently reproduced and fixed; its terminal status is cancelled, not passed.

Heritage `7391f2b` bounds startup, boot checks, instrumentation and cleanup commands, including forced timeout termination. A missing-emulator fault injection reproduced the original cleanup hang; the corrected runner exits with the expected failure and retains its emulator log. The test shortens bounded calls to 0.3 seconds; its roughly two-second completion is a test result, not the production timeout setting. That regression check is included in the workflow before SDK setup.

Heritage `d8e5d438ff345a4d27f35765d6aa9a739f1b2e17` uses an explicit temporary Android user/AVD directory shared by the SDK tools and emulator, chooses avdmanager beside the configured sdkmanager, requires the AVD registration file and retains the AVD-creation log. [Android's environment-variable reference](https://developer.android.com/tools/variables) defines the relevant directory lookup behavior.

The corrected [package-only run 34771300423](https://github.com/edydex/heritage_study_bible/actions/runs/34771300423) started the emulator and passed all three Community integration tests. The run failed on the pre-existing Capacitor sample test, which expected `com.getcapacitor.app` rather than the actual `faith.heritage.app`. Its logs also show that Gradle uninstalled the app before cleanup could pull the screenshot files.

Heritage `181434390ca374b7baeb6978bdf29be689027f5b` corrects the sample assertion, retains the test APK until screenshots are copied, and requires all three PNGs before verifying the package. [Package-only run 34771753817](https://github.com/edydex/heritage_study_bible/actions/runs/34771753817) passed instrumentation and retained screenshots, but failed because the verifier expected website-only `.well-known/assetlinks.json` inside the APK. Visual inspection found that all three screenshots were covered by a Pixel Launcher non-response dialog and their rendered backgrounds lagged the JavaScript route assertions. These screenshots were not accepted.

Heritage `fc8d673f1250fecfef676e597b2327a91908099d` requires the active native accessibility window to belong to Heritage and show the expected heading before capturing each screenshot. It also explicitly checks that the emulator is offline, increases emulator resources, and prebuilds instrumentation before emulator startup. The asset verifier now checks the website association against the actual APK signer while still comparing every runtime web asset byte for byte. [Run 34772285775](https://github.com/edydex/heritage_study_bible/actions/runs/34772285775) failed before emulator startup: the unscoped prebuild also selected Capacitor plugin test apps, one of which has conflicting Kotlin test dependencies.

Heritage `d0fd1cc94594ab4732a9ef910876b7b4931684b7` scopes both prebuild tasks to `:app`, matching the existing scoped instrumentation command. [Run 34772637892](https://github.com/edydex/heritage_study_bible/actions/runs/34772637892), requested immediately after the push, actually resolved to the prior `fc8d673` commit and repeated its unscoped build failure. The run metadata and build log confirm that it did not test `d0fd1cc`. After the remote ref and workflow contents both showed the corrected commit, [package-only run 34772937197](https://github.com/edydex/heritage_study_bible/actions/runs/34772937197) was started. Its run metadata explicitly confirms `d0fd1cc94594ab4732a9ef910876b7b4931684b7`; that run built both app APKs and reached native instrumentation. The two encrypted-storage tests and package identity test passed. The offline-screen test failed before taking screenshots because `navigator.onLine` remained true; logcat showed a connected emulated cellular network. No screenshot from that run was accepted.

Heritage `3a083bc873f59d1de7410c509c641b6bb8555088` adds normal Android network-state permission and checks native ConnectivityManager as well as WebView state. The runner enables airplane mode, unregisters the emulated data connection and retains connectivity diagnostics. The [Android emulator console reference](https://developer.android.com/studio/run/emulator-console) documents this modem control.

The following reader fix, `94a87a486986ee16e1ece011253db99b69816d46`, also supplies a direct church-member sign-in recovery action when a saved membership has no usable session. Personal sync is separate. Its 192 unit tests, 105 protocol tests and a production-browser recovery test passed locally; the synthetic test intercepts the email request.

[Package-only run 34773580184](https://github.com/edydex/heritage_study_bible/actions/runs/34773580184), source `94a87a486986ee16e1ece011253db99b69816d46`, passed both native and WebView offline checks but failed the accessibility heading lookup. The retained failure screenshot visibly shows the correct Community Home with no launcher dialog. The screen check itself was therefore rejecting a correctly rendered page. Full frontend [CI 34773619388](https://github.com/edydex/heritage_study_bible/actions/runs/34773619388) passed, and [web deployment 34773736993](https://github.com/edydex/heritage_study_bible/actions/runs/34773736993) published the member recovery fix.

Heritage `9681d009497f0b138b59247e3457abe57a4f14d0` replaces the unreliable virtual accessibility-text lookup with WebView visual-state completion, native window focus and visible view bounds. It retains the DOM heading assertion, the no-network checks and screenshots. [WebView documentation](https://developer.android.com/reference/android/webkit/WebView#postVisualStateCallback(long,%20android.webkit.WebView.VisualStateCallback)) defines the rendered-frame callback. [Package-only run 34773992937](https://github.com/edydex/heritage_study_bible/actions/runs/34773992937) passed CI, all four instrumentation tests, signer/update compatibility and 531 packaged web-asset comparisons. Independent download checks verified the APK/metadata hashes. Visual inspection nevertheless found that `sermon-archive.png` duplicated the previous Community Home frame. The other two captures showed their expected screens. The APK was not published.

Heritage `92468e477ff0af76cc90ee5ac85a5c318147bc52` schedules a draw after the WebView callback and waits for the captured content area to differ from the previous screen, excluding system clock/navigation areas. It fails after a bounded wait if the capture stays stale. [Package-only run 34774473758](https://github.com/edydex/heritage_study_bible/actions/runs/34774473758) failed before screen capture: native connectivity still had active network 100. Its pre-test connectivity dump had no active network, but logcat proves `com.google.android.googlesdksetup` enabled Wi-Fi after the runner disabled it.

Heritage `17a9713fa97c1a752def224259469c41446987d8` disables that first-boot setup component only in the runner-created disposable emulator before turning off its radios. The actual app/device settings are unchanged. [Package-only run 34774827568](https://github.com/edydex/heritage_study_bible/actions/runs/34774827568) passed the offline checks but correctly rejected a window obscured by a Pixel Launcher crash dialog. Logcat places the launcher input-dispatch timeout before Heritage instrumentation began. Heritage `782535d41e870e12983f807d7f8dda2e217e2c56` replaces the early MENU key injection with `wm dismiss-keyguard`, avoiding key dispatch to the still-starting launcher. [Package-only run 34775207954](https://github.com/edydex/heritage_study_bible/actions/runs/34775207954) completed successfully at that exact source. Workflow publication was disabled so the actual reports and screenshots could be inspected first. All four tests, three distinct screen captures, signer/version checks and 531 runtime web-asset comparisons passed acceptance. The checked-in publisher then tagged that source and uploaded the exact artifact. An independent download of all three release assets matched both GitHub digests and the accepted artifact byte for byte; v1.1.32 remains the latest stable release. The APK runtime bytes match the earlier green run, because subsequent changes corrected the tests and emulator runner. The earlier screenshots remain rejected.

No real account, email, microphone or paid translation provider is used by these emulator checks. WOTBC and the existing browser deployment were not changed.
