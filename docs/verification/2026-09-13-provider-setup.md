# Existing-provider setup — 2026-09-13

The original Multilinguum processor already has OpenAI and LiveKit credentials. Its Cloudflare SSH route timed out, but its documented LAN address `192.168.1.26` connected with strict verification against the existing `ssh-redact.mayos.dev` host key. The host identifies as `video-redactor-gpu` on Debian 13.7. Its private configuration remains mode `0600`.

The existing OpenAI key successfully retrieved model metadata for `gpt-6-astra`, `gpt-live-transcribe` and `gpt-4o-mini-tts`. This establishes metadata access; paid inference remains untested. The relay configuration names a secure LiveKit Cloud endpoint and contains non-placeholder credentials, but relay access has not yet been verified. No credential values were displayed, no sharing setting changed, and no billable request was made. [Exact observations](2026-09-13-provider-setup.json).

WOTBC's separately verified Debian server has a healthy translation processor at the selected Multilinguum revision, with OpenAI credentials still absent. The prepared transfer uses two trusted SSH connections and process pipes to pass only the existing provider/relay values into `heritage-community translation configure --non-interactive --yes`. That supported command handles the safety backup, private configuration, restart and endpoint checks. It preserves Economy settings and does not start a translation session.

Automatic approval review rejected execution because the credential transfer to WOTBC needs explicit destination authorization. The command did not start, and WOTBC's provider configuration remains unchanged. An explicit approval request is pending with the user. The prepared script is retained privately under `.heritage/rehearsals/2026-09-13/providers/`; it contains no credentials and must not be run until approval arrives.

Two original synthetic recordings are ready for live capture testing: English, 10.49 seconds, and Russian, 12.59 seconds. They use installed macOS voices, 48 kHz mono signed 16-bit PCM, and a short Ephesians/grace/faith passage. File hashes, exact text and non-silent audio measurements are in the observation record. The files are retained alongside the setup script. No private sermon recording or voice clone is involved.

After approval, apply the prepared setup, check processor/profile/relay readiness, and reserve a conservative cost in [the test budget](api-test-budget.json) before each billable test. Check one English/Russian target at a time, inspect the translations before expanding the run, then verify speech-on, speech-off and the live pages with the short recordings. The configured Quality model's current [pricing and supported reasoning settings](https://developers.openai.com/api/docs/models/gpt-6-astra), [live transcription rate](https://developers.openai.com/api/docs/models/gpt-live-transcribe) and [speech pricing](https://developers.openai.com/api/docs/models/gpt-4o-mini-tts) were checked against the official documentation.

The WOTBC manager login and real phone, mixer, tablet and physical display acceptance remain separate. The paid-test ledger is still **$0 used / $20 authorized**, with no pending billable request.
